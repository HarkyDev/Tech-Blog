const router = require("express").Router();
const { User } = require('../..')
// const withAuth - require(utils)


router.post("/signup", async (req, del) => {

    try {

    } catch (err){

    }
})


router.post("/login", async (req, del) => {

    try {

    } catch (err){
        
    }
})



router.post("/logout", async (req, del) => {

    try {

    } catch (err){
        
    }
})